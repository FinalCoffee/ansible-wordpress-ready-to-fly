sudo: true
services:
    - docker

before_install:
    - docker info
    - docker version

    - docker build  -f test/Dockerfile  -t nginx_trusty   .

script:
    - docker run  -v $(pwd):/data  nginx_trusty

    - echo "==> Validating the test results..."
    - grep '<title>ANSIBLE_TEST' result-wordpress


notifications:
    webhooks: https://galaxy.ansible.com/api/v1/notifications/
